#!/bin/sh

BASEDIR=$(pwd)
folder="$BASEDIR/"

cd $folder
git checkout master
git pull
git checkout production-built
git pull origin production-built
git merge --no-ff master
npm i && npm run build
#git commit -a -m "Updating build following PR #$1"
#git push origin production-built
